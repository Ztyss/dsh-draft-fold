# Add this bundle as a browser-capable DSH plugin.
# The profile may override threshold and previewChars in a later patch layer.
- insert:
    - id: "@ztyss/dsh-draft-fold"
      name: "@ztyss/dsh-draft-fold"
      config:
        threshold: 2000
        previewChars: 120
